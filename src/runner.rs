//use std::process::Command;

pub fn _run(file_ext: &str, _file_name: &String) {
    match file_ext {
        "cpp" => {
            //let Command::new("g++").Command::arg("{file_name}").arg("-o").arg("output").output();
            todo!()
        }
        _ => {
            todo!()
        }
    }
}
